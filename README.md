# webgpu-test

This example invoke N times a compute shader to emit N triangles stored into a SSBO (Shader Storage Buffer Object). SSBO was binded to a VBO (Vertex Buffer Object). Therefore, the vertex shader takes as input the triangles previously generated by the compute shader. 

# Build & Run

```
# Configure
cmake -B build
# Build
cmake --build build --parallel
# Run
./build/webgpu_minimal
```

# Thanks to

Initialization of WebGPU was guided by the amazing tutorial:
https://eliemichel.github.io/LearnWebGPU